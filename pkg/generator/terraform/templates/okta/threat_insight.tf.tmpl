# Okta ThreatInsight — generated by Bosun
# Enables ThreatInsight in block mode to automatically block known-malicious IPs.

resource "okta_threat_insight_settings" "block" {
  action = "block"

  # Optionally exclude trusted network zones from blocking.
  # Uncomment and add zone IDs if you have VPN/office IPs that might
  # trigger false positives.
  # exclude_zones = [okta_network_zone.office.id]
}
